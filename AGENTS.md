# Codex Instructions

- Do not rename the default branch from `master` to `main`.
- Keep commits separated by logical entity so each area can be rolled back independently.
- For this dotfiles repo, prefer separate commits for repository metadata, Hyprland, Alacritty, Fish, and Vim changes.
